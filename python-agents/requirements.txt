# Pinned to the versions this app is actually verified running against.
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.12.5
python-dotenv==1.2.2
httpx==0.28.1

# Data layer. SQLite by default; set DATABASE_URL to move to Postgres
# (add psycopg[binary] when you do).
sqlalchemy==2.0.52

# Per-IP rate limiting on the Gemini-backed and escrow routes.
slowapi==0.1.10

# The code prefers the new SDK (`from google import genai`) and falls back to the
# legacy one only if that import fails. Both are declared so behaviour is identical
# on a clean install; google-genai is the path actually exercised.
google-genai==2.4.0
google-generativeai==0.8.6
