==========================================================================
KHIDMAT AI — FINAL DEBUG + FULL ADVANCEMENT + APK DELIVERY SUPER PROMPT
Version 2.0 | Production Ready | Zero Bug Tolerance
==========================================================================

YOU ARE IN FINAL PRODUCTION MODE.
Your single mission: debug everything, advance everything, test everything,
deliver a signed working APK uploaded to Google Drive and GitHub.
No shortcuts. No skipped steps. No silent failures.

==========================================================================
PHASE 0 — FULL SYSTEM DIAGNOSTIC (MANDATORY FIRST STEP)
==========================================================================

Run ALL of the following before touching any code.
Log every result. Fix every failure before moving to Phase 1.

── ENVIRONMENT CHECK ──────────────────────────────────────────────────────

  node --version              → Must be >= 18.0.0
  npm --version               → Must be >= 9.0.0
  expo --version              → Must be >= 50.0.0
  npx react-native doctor     → All checkmarks must be green
  npx expo-doctor             → Zero warnings allowed
  adb devices                 → Must show connected device or emulator
  firebase --version          → Must be >= 12.0.0
  java -version               → Must be JDK 17
  echo $ANDROID_HOME          → Must return valid SDK path

  If ANY check fails, fix it completely before proceeding.
  Do not proceed with a broken environment.

── EXPO APP NOT STARTING — FIX SEQUENCE ──────────────────────────────────

  Execute in this exact order:

  STEP 1: Nuclear cache clear
    rm -rf node_modules
    rm -rf .expo
    rm -rf /tmp/metro-*
    rm package-lock.json
    watchman watch-del-all
    npm cache clean --force
    npm install
    npx expo install --check

  STEP 2: Fix all package version mismatches
    For every warning from "npx expo install --check":
    Run: npx expo install [package-name]
    NEVER use plain npm install for Expo packages
    NEVER mix Expo SDK versions in same project

  STEP 3: Start with full reset
    npx expo start --clear --reset-cache

  STEP 4: If app still not loading on mobile device
    Check phone and laptop are on IDENTICAL WiFi network
    Disable Windows Firewall / Mac Firewall temporarily for testing
    Try tunnel mode: npx expo start --tunnel
    Install if needed: npm install -g @expo/ngrok

  STEP 5: If tunnel fails
    Use direct IP: npx expo start --host [your-local-IP]
    Find IP via: ipconfig (Windows) or ifconfig (Mac/Linux)

  STEP 6: Test on emulator if physical device fails
    Android: Open Android Studio → Device Manager → Start Emulator
    Then: npx expo start → press 'a' for Android

── COMMON BUG AUTO-FIX LIST ──────────────────────────────────────────────

  BUG: "Unable to resolve module X"
  FIX: npx expo install X
       If still fails: check spelling in import statement
       Check if module needs babel plugin (add to babel.config.js)

  BUG: White/blank screen on device
  FIX: Add this to App.tsx root:
         <ErrorBoundary>
           <NavigationContainer>
             ...
           </NavigationContainer>
         </ErrorBoundary>
       Check ALL screen components return valid JSX (not null)
       Check no undefined variables at render time

  BUG: Metro bundler port conflict
  FIX: npx expo start --port 8082
       Or kill process: npx kill-port 8081

  BUG: Firebase "app not initialized" error
  FIX: Move initializeApp() to top of firebase.ts
       Import firebase.ts before any other service file in App.tsx
       Verify google-services.json is at /android/app/google-services.json

  BUG: "Navigation container not found"
  FIX: Ensure single <NavigationContainer> wraps entire app
       Never nest NavigationContainers
       Check DrawerNavigator is inside NavigationContainer

  BUG: Fonts not rendering / showing boxes
  FIX: 
    import * as Font from 'expo-font';
    import { useFonts } from 'expo-font';
    const [fontsLoaded] = useFonts({
      'NotoNastaliqUrdu': require('./assets/fonts/NotoNastaliqUrdu.ttf'),
      'Poppins-Regular': require('./assets/fonts/Poppins-Regular.ttf'),
      'Poppins-Bold': require('./assets/fonts/Poppins-Bold.ttf'),
    });
    if (!fontsLoaded) return <LoadingScreen />;

  BUG: APK crashes on launch
  FIX: Run: cd android && ./gradlew clean
       Run: cd android && ./gradlew bundleRelease --stacktrace
       Add to android/app/proguard-rules.pro:
         -keep class com.google.firebase.** { *; }
         -keep class expo.modules.** { *; }
         -keep class com.facebook.react.** { *; }
         -dontwarn com.google.**

  BUG: API calls returning 403/401
  FIX: Check all API keys in .env file
       Verify .env is loaded via react-native-dotenv or expo constants
       Check Cloud Run service allows unauthenticated requests (for demo)
       Add CORS headers to all backend services

  BUG: Sidebar/drawer not opening
  FIX: Install: npx expo install @react-navigation/drawer
       Install: npx expo install react-native-gesture-handler
       Install: npx expo install react-native-reanimated
       Add to top of App.tsx: import 'react-native-gesture-handler';
       Wrap app in <GestureHandlerRootView style={{flex:1}}>

  BUG: Animations laggy or not running
  FIX: Add to babel.config.js plugins:
         ['react-native-reanimated/plugin']
       Use useNativeDriver: true in all Animated calls
       Use Reanimated 2 for complex animations

  BUG: Voice input not working
  FIX: npx expo install expo-speech expo-av
       Add to app.json permissions:
         "android": {
           "permissions": [
             "RECORD_AUDIO",
             "INTERNET",
             "VIBRATE"
           ]
         }

  BUG: Tracking/receipt screen showing empty data
  FIX: Check booking object is passed correctly via navigation params
       Use: route.params?.bookingId to fetch from Firestore
       Add loading state while fetching booking data
       Add fallback UI if booking data is null

==========================================================================
PHASE 1 — BACKEND VERIFICATION & DEBUGGING
==========================================================================

── AGENT PIPELINE VERIFICATION ───────────────────────────────────────────

Test every single agent endpoint. Fix any that fail before moving on.
Log pass/fail for each.

TEST AGENT 1 — INTENT_AGENT:
  Endpoint: POST /parse-intent
  
  Test Case A (Roman Urdu):
  curl -X POST [INTENT_SERVICE_URL]/parse-intent \
    -H "Content-Type: application/json" \
    -d '{
      "text": "Mujhe kal subah G-13 mein AC technician chahiye",
      "session_id": "debug-001"
    }'
  Must return:
    service_type: "AC Technician"
    location: "G-13"
    time_preference: "tomorrow morning"
    urgency_level: "medium"
    confidence_score: >= 0.80
    language_detected: "roman_urdu"
    clarification_needed: false

  Test Case B (Low confidence — ambiguous):
  curl -X POST [INTENT_SERVICE_URL]/parse-intent \
    -H "Content-Type: application/json" \
    -d '{
      "text": "koi banda chahiye ghr k liye",
      "session_id": "debug-002"
    }'
  Must return:
    confidence_score: < 0.75
    clarification_needed: true
    clarification_question: [non-null string in same language]

  Test Case C (Emergency Urdu):
  curl -X POST [INTENT_SERVICE_URL]/parse-intent \
    -H "Content-Type: application/json" \
    -d '{
      "text": "پانی کا پائپ پھٹ گیا ہے ابھی فوری مدد چاہیے",
      "session_id": "debug-003"
    }'
  Must return:
    service_type: "Plumber"
    urgency_level: "emergency"
    confidence_score: >= 0.85

  FIXES IF INTENT AGENT FAILS:
    Verify Claude API key: echo $CLAUDE_API_KEY
    Check Claude model string is exactly: claude-sonnet-4-20250514
    Check system prompt instructs Claude to return ONLY valid JSON
    Add JSON parse error handling:
      try {
        const parsed = JSON.parse(response);
      } catch(e) {
        // Extract JSON from markdown code blocks if present
        const match = response.match(/```json\n?([\s\S]*?)\n?```/);
        if (match) return JSON.parse(match[1]);
      }

TEST AGENT 2 — PROVIDER_DISCOVERY_AGENT:
  Endpoint: POST /match-providers
  
  curl -X POST [DISCOVERY_SERVICE_URL]/discover-providers \
    -H "Content-Type: application/json" \
    -d '{
      "service_type": "AC Technician",
      "location": "G-13, Islamabad",
      "lat": 33.6844,
      "lng": 73.0479,
      "session_id": "debug-001"
    }'
  Must return: Array of >= 5 providers
  Each provider must have ALL these fields (check individually):
    provider_id ✓  name ✓  lat ✓  lng ✓
    distance_km ✓  rating ✓  on_time_score ✓
    cancellation_rate ✓  base_rate_pkr ✓
    availability_slots ✓  specialization_tags ✓
    risk_score ✓  capacity_today ✓

  FIXES IF DISCOVERY FAILS:
    Verify Firestore has seeded provider data:
      firebase firestore:get providers (check not empty)
    If empty: trigger seed function:
      curl [FUNCTIONS_URL]/seedProviders
    Verify Google Maps Geocoding API is enabled in GCP console
    Check API key has no referrer restrictions blocking Cloud Run

TEST AGENT 3 — MATCHING_AGENT:
  Endpoint: POST /rank-providers
  
  curl -X POST [MATCHING_SERVICE_URL]/rank-providers \
    -H "Content-Type: application/json" \
    -d '{
      "providers": [...from discovery...],
      "intent": {...from intent agent...},
      "session_id": "debug-001"
    }'
  Must return: Top 3 ranked providers where:
    - Closest provider is NOT automatically #1
    - Each provider has score_breakdown with 8 factor scores
    - total_score between 0-100 for each
    - reasoning_text is human-readable plain language string
    - #1 ranked has highest total_score

  CRITICAL CHECK: Verify stress-test scenario is working:
    Provider X (closest, cheap, cancellation_rate=0.42) must rank LOWER
    than Provider Y (farther, higher rate, on_time_score=0.96)
    If X ranks above Y, fix the cancellation_rate weight factor

TEST AGENT 4 — PRICING_AGENT:
  Endpoint: POST /generate-quote

  curl -X POST [PRICING_SERVICE_URL]/generate-quote \
    -H "Content-Type: application/json" \
    -d '{
      "provider": {...top ranked provider...},
      "intent": {...intent object...},
      "session_id": "debug-001"
    }'
  Must return:
    base_fee: number > 0
    distance_fee: number >= 0
    urgency_adjustment: number
    complexity_adjustment: number
    surge_fee: number >= 0
    loyalty_discount: number (0.95 if returning user, else 1.0)
    total_estimate_pkr: number (sum of all components)
    quote_range: { min: total*0.9, max: total*1.1 }
    budget_alternative: object if budget_sensitivity = "high"
    breakdown_text: non-empty string

  FIXES IF PRICING FAILS:
    Check all multipliers defined for ALL enum values:
      urgency: low/medium/high/emergency
      complexity: basic/intermediate/complex
    Check no NaN or Infinity in calculation (add Number.isFinite checks)
    If breakdown_text is empty, fix Claude prompt to always return it

TEST AGENT 5 — BOOKING_AGENT:
  Endpoint: POST /create-booking

  curl -X POST [BOOKING_SERVICE_URL]/create-booking \
    -H "Content-Type: application/json" \
    -d '{
      "provider_id": "provider_002",
      "slot_datetime": "2025-01-16T10:00:00+05:00",
      "user_id": "test_user_001",
      "service_type": "AC Repair",
      "location": "G-13, Islamabad",
      "price_quote_pkr": 2800,
      "session_id": "debug-001"
    }'
  Must return:
    booking_id: non-empty string (format: BK-XXXXXX)
    status: "confirmed"
    slot_datetime: echoed back
    provider_name: string
    confirmation_message: string in user language
    receipt_text: formatted booking receipt
    simulated_notifications: array with user+provider entries

  VERIFY IN FIRESTORE:
    Check bookings collection has new document
    Check providers/{id} availability_slots updated (slot removed)
    Check agent_logs has entry for this session_id with agent: BOOKING_AGENT

  DOUBLE-BOOKING PREVENTION TEST:
    Send identical booking request twice simultaneously
    Second request must return:
      status: "conflict"
      message: "Slot already booked"
      alternative_slots: array of next 3 available times

TEST AGENT 6 — FOLLOWUP_AGENT:
  Verify Firebase scheduled functions exist:
    firebase functions:list
    Must show: scheduleReminder, sendEnRouteUpdate, requestFeedback
  
  Manually trigger reminder test:
    curl [FUNCTIONS_URL]/testReminder?booking_id=BK-TEST001
    Must log: "Reminder simulated for BK-TEST001"
  
  Verify Firestore booking status transitions:
    "confirmed" → "reminder_sent" → "in_progress" → "completed"
    Each transition must update booking document AND agent_logs

TEST AGENT 7 — DISPUTE_AGENT:
  Endpoint: POST /create-dispute
  
  Test no-show scenario:
  curl -X POST [DISPUTE_URL]/create-dispute \
    -H "Content-Type: application/json" \
    -d '{
      "booking_id": "BK-TEST001",
      "type": "no_show",
      "reported_by": "user",
      "description": "Provider did not arrive at scheduled time"
    }'
  Must return:
    dispute_id: non-empty string
    resolution_action: "auto_reschedule"
    penalty_applied: true
    replacement_provider: object (next ranked provider)
    user_message: non-empty apology + rebook message

  Test price dispute:
  curl -X POST [DISPUTE_URL]/create-dispute \
    -H "Content-Type: application/json" \
    -d '{
      "booking_id": "BK-TEST001",
      "type": "price_dispute",
      "quoted_amount": 2500,
      "charged_amount": 3200
    }'
  Must return:
    overcharge_detected: true (>10% difference)
    refund_amount: 700
    resolution_action: "partial_refund_simulated"

── FULL PIPELINE END-TO-END TEST ─────────────────────────────────────────

  Run this single orchestrator call and verify ALL agents fire:
  
  curl -X POST [ORCHESTRATOR_URL]/orchestrate \
    -H "Content-Type: application/json" \
    -d '{
      "user_input": "AC bilkul kaam nahi kar raha kal subah G-13 mein
                     technician chahiye budget zyada nahi",
      "user_id": "hackathon_demo_user",
      "session_id": "pipeline-test-final"
    }'

  Verify in Firestore agent_logs for session "pipeline-test-final":
    Entry 1: agent_name: "INTENT_AGENT" ✓
    Entry 2: agent_name: "PROVIDER_DISCOVERY_AGENT" ✓
    Entry 3: agent_name: "MATCHING_AGENT" ✓
    Entry 4: agent_name: "PRICING_AGENT" ✓
    Entry 5: agent_name: "BOOKING_AGENT" ✓
  
  Total pipeline latency must be < 15 seconds
  If > 15 seconds: run agents 2+3 in parallel using Promise.all()

── PIPELINE PARALLELIZATION (IF NEEDED) ──────────────────────────────────

  In orchestrator-service/index.js, optimize like this:

  // Sequential (slow):
  const intent = await callIntentAgent(input);
  const providers = await callDiscoveryAgent(intent);
  const ranked = await callMatchingAgent(providers, intent);
  const quote = await callPricingAgent(ranked[0], intent);

  // Optimized (fast) — run matching + pricing in parallel:
  const intent = await callIntentAgent(input);
  const providers = await callDiscoveryAgent(intent);
  const [ranked, initialQuote] = await Promise.all([
    callMatchingAgent(providers, intent),
    callPricingAgent(providers[0], intent) // pre-fetch for top candidate
  ]);
  const finalQuote = await callPricingAgent(ranked[0], intent);

── BACKEND CORS & SECURITY ───────────────────────────────────────────────

  Add to ALL Cloud Run services (app.js / index.js):

  const cors = require('cors');
  app.use(cors({
    origin: '*', // For demo/hackathon only
    methods: ['GET', 'POST', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization']
  }));
  app.use(express.json({ limit: '10mb' }));
  
  Add health check to every service:
  app.get('/health', (req, res) => {
    res.json({ status: 'ok', service: 'intent-service', 
               timestamp: new Date().toISOString() });
  });

==========================================================================
PHASE 2 — MOBILE APP FULL REBUILD & ADVANCEMENT
==========================================================================

── COMPLETE FILE STRUCTURE ───────────────────────────────────────────────

Implement exactly this structure. Create any missing files.

/KhidmatAI
  App.tsx                          ← Root with theme + navigation
  app.json                         ← Expo config
  babel.config.js                  ← With reanimated plugin
  .env                             ← API keys (gitignored)
  /assets
    /fonts
      Poppins-Regular.ttf
      Poppins-Bold.ttf
      Poppins-SemiBold.ttf
      NotoNastaliqUrdu-Regular.ttf
    /images
      logo.png
      logo-dark.png
  /src
    /components
      LoadingScreen.tsx
      Sidebar.tsx
      AgentTraceViewer.tsx
      ProviderCard.tsx
      PriceBreakdown.tsx
      BookingReceipt.tsx
      StatusTimeline.tsx
      ErrorBoundary.tsx
      ThemeToggle.tsx
      AnimatedHeader.tsx
      SkeletonLoader.tsx
      VoiceInputButton.tsx
      ConfidenceBadge.tsx
      NotificationPreview.tsx
      EmptyState.tsx
      ToastMessage.tsx
    /screens
      SplashScreen.tsx
      OnboardingScreen.tsx
      HomeScreen.tsx
      ProcessingScreen.tsx
      ResultsScreen.tsx
      BookingConfirmScreen.tsx
      TrackingScreen.tsx
      AgentLogsScreen.tsx
      ProfileScreen.tsx
      DisputeScreen.tsx
      SettingsScreen.tsx
    /navigation
      AppNavigator.tsx
      DrawerNavigator.tsx
    /theme
      colors.ts
      typography.ts
      spacing.ts
      ThemeContext.tsx
    /hooks
      useTheme.ts
      useOrchestrator.ts
      useBooking.ts
      useVoiceInput.ts
      useAgentLogs.ts
    /services
      api.ts
      firebase.ts
      storage.ts
    /store
      bookingStore.ts
      userStore.ts
    /utils
      languageDetector.ts
      priceFormatter.ts
      dateFormatter.ts
      errorHandler.ts

── THEME SYSTEM — IMPLEMENT FULLY ────────────────────────────────────────

File: /src/theme/colors.ts

export const lightColors = {
  primary: '#006D77',
  primaryDark: '#004C55',
  primaryLight: '#83C5BE',
  accent: '#F4A261',
  accentDark: '#E76F51',
  background: '#FAFAFA',
  surface: '#FFFFFF',
  surfaceElevated: '#F0F4F4',
  text: '#1A1A2E',
  textSecondary: '#4A5568',
  textMuted: '#9AA5B1',
  border: '#E2E8F0',
  success: '#38A169',
  warning: '#D69E2E',
  error: '#E53E3E',
  info: '#3182CE',
  divider: '#EDF2F7',
  shadow: 'rgba(0,0,0,0.08)',
  overlay: 'rgba(0,0,0,0.5)',
  cardBg: '#FFFFFF',
  inputBg: '#F7FAFC',
  agentActive: '#006D77',
  agentComplete: '#38A169',
  agentPending: '#CBD5E0',
};

export const darkColors = {
  primary: '#83C5BE',
  primaryDark: '#006D77',
  primaryLight: '#004C55',
  accent: '#F4A261',
  accentDark: '#E76F51',
  background: '#0D1117',
  surface: '#161B22',
  surfaceElevated: '#21262D',
  text: '#F0F6FC',
  textSecondary: '#8B949E',
  textMuted: '#484F58',
  border: '#30363D',
  success: '#3FB950',
  warning: '#D29922',
  error: '#F85149',
  info: '#58A6FF',
  divider: '#21262D',
  shadow: 'rgba(0,0,0,0.3)',
  overlay: 'rgba(0,0,0,0.7)',
  cardBg: '#161B22',
  inputBg: '#0D1117',
  agentActive: '#83C5BE',
  agentComplete: '#3FB950',
  agentPending: '#30363D',
};

File: /src/theme/ThemeContext.tsx

import React, { createContext, useContext, useState, useEffect } from 'react';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { lightColors, darkColors } from './colors';

type Theme = 'light' | 'dark';
interface ThemeContextType {
  theme: Theme;
  colors: typeof lightColors;
  toggleTheme: () => void;
  isDark: boolean;
}

const ThemeContext = createContext<ThemeContextType>({} as ThemeContextType);

export const ThemeProvider = ({ children }: { children: React.ReactNode }) => {
  const [theme, setTheme] = useState<Theme>('light');
  
  useEffect(() => {
    AsyncStorage.getItem('khidmat_theme').then(saved => {
      if (saved === 'dark' || saved === 'light') setTheme(saved);
    });
  }, []);

  const toggleTheme = async () => {
    const next = theme === 'light' ? 'dark' : 'light';
    setTheme(next);
    await AsyncStorage.setItem('khidmat_theme', next);
  };

  return (
    <ThemeContext.Provider value={{
      theme,
      colors: theme === 'dark' ? darkColors : lightColors,
      toggleTheme,
      isDark: theme === 'dark'
    }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => useContext(ThemeContext);

── LOADING SCREEN — IMPLEMENT FULLY ──────────────────────────────────────

File: /src/components/LoadingScreen.tsx

import React, { useEffect, useRef } from 'react';
import { 
  View, Text, Animated, StyleSheet, 
  Dimensions, StatusBar 
} from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { useTheme } from '../theme/ThemeContext';

const { width, height } = Dimensions.get('window');

export const LoadingScreen = ({ onFinish }: { onFinish?: () => void }) => {
  const { isDark } = useTheme();
  const logoScale = useRef(new Animated.Value(0.8)).current;
  const logoOpacity = useRef(new Animated.Value(0)).current;
  const taglineOpacity = useRef(new Animated.Value(0)).current;
  const subtitleOpacity = useRef(new Animated.Value(0)).current;
  const dot1 = useRef(new Animated.Value(0)).current;
  const dot2 = useRef(new Animated.Value(0)).current;
  const dot3 = useRef(new Animated.Value(0)).current;
  const progressWidth = useRef(new Animated.Value(0)).current;

  useEffect(() => {
    // Logo entrance
    Animated.parallel([
      Animated.spring(logoScale, {
        toValue: 1,
        tension: 50,
        friction: 8,
        useNativeDriver: true,
      }),
      Animated.timing(logoOpacity, {
        toValue: 1,
        duration: 600,
        useNativeDriver: true,
      }),
    ]).start(() => {
      // Tagline fade in
      Animated.sequence([
        Animated.timing(taglineOpacity, {
          toValue: 1, duration: 500, useNativeDriver: true,
        }),
        Animated.timing(subtitleOpacity, {
          toValue: 1, duration: 400, useNativeDriver: true,
        }),
      ]).start();
    });

    // Logo pulse loop
    Animated.loop(
      Animated.sequence([
        Animated.timing(logoScale, {
          toValue: 1.06, duration: 1200, useNativeDriver: true,
        }),
        Animated.timing(logoScale, {
          toValue: 1.0, duration: 1200, useNativeDriver: true,
        }),
      ])
    ).start();

    // Dots animation
    const dotAnimation = (dot: Animated.Value, delay: number) =>
      Animated.loop(
        Animated.sequence([
          Animated.delay(delay),
          Animated.timing(dot, {
            toValue: 1, duration: 400, useNativeDriver: true,
          }),
          Animated.timing(dot, {
            toValue: 0, duration: 400, useNativeDriver: true,
          }),
        ])
      );

    dotAnimation(dot1, 0).start();
    dotAnimation(dot2, 200).start();
    dotAnimation(dot3, 400).start();

    // Progress bar
    Animated.timing(progressWidth, {
      toValue: width * 0.7,
      duration: 3000,
      useNativeDriver: false,
    }).start();

    // Auto-finish after 3.5 seconds
    const timer = setTimeout(() => {
      onFinish?.();
    }, 3500);

    return () => clearTimeout(timer);
  }, []);

  const dotStyle = (anim: Animated.Value) => ({
    width: 8, height: 8, borderRadius: 4,
    backgroundColor: '#F4A261',
    marginHorizontal: 4,
    opacity: anim,
    transform: [{
      translateY: anim.interpolate({
        inputRange: [0, 1],
        outputRange: [0, -8],
      })
    }]
  });

  return (
    <LinearGradient
      colors={isDark 
        ? ['#0D1117', '#004C55', '#0D1117'] 
        : ['#006D77', '#004C55', '#006D77']}
      style={styles.container}
      start={{ x: 0, y: 0 }}
      end={{ x: 1, y: 1 }}
    >
      <StatusBar barStyle="light-content" backgroundColor="transparent" translucent />
      
      <Animated.View style={[styles.logoContainer, {
        opacity: logoOpacity,
        transform: [{ scale: logoScale }]
      }]}>
        {/* Logo circle with KA initials */}
        <View style={styles.logoCircle}>
          <Text style={styles.logoText}>خ</Text>
        </View>
        <Text style={styles.appName}>KHIDMAT AI</Text>
      </Animated.View>

      <Animated.Text style={[styles.taglineUrdu, { opacity: taglineOpacity }]}>
        آپ کی خدمت میں حاضر ہیں
      </Animated.Text>

      <Animated.Text style={[styles.taglineEn, { opacity: subtitleOpacity }]}>
        AI Service Orchestrator for Pakistan
      </Animated.Text>

      <View style={styles.dotsContainer}>
        <Animated.View style={dotStyle(dot1)} />
        <Animated.View style={dotStyle(dot2)} />
        <Animated.View style={dotStyle(dot3)} />
      </View>

      <View style={styles.progressContainer}>
        <Animated.View style={[styles.progressBar, { width: progressWidth }]} />
      </View>

      <Animated.Text style={[styles.loadingText, { opacity: subtitleOpacity }]}>
        Initializing AI Agents...
      </Animated.Text>
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1, alignItems: 'center', justifyContent: 'center',
  },
  logoContainer: {
    alignItems: 'center', marginBottom: 24,
  },
  logoCircle: {
    width: 100, height: 100, borderRadius: 50,
    backgroundColor: 'rgba(255,255,255,0.15)',
    borderWidth: 2, borderColor: 'rgba(244,162,97,0.8)',
    alignItems: 'center', justifyContent: 'center',
    marginBottom: 16,
  },
  logoText: {
    fontSize: 48, color: '#F4A261', fontFamily: 'NotoNastaliqUrdu-Regular',
  },
  appName: {
    fontSize: 28, fontFamily: 'Poppins-Bold',
    color: '#FFFFFF', letterSpacing: 4,
  },
  taglineUrdu: {
    fontSize: 18, color: 'rgba(255,255,255,0.9)',
    fontFamily: 'NotoNastaliqUrdu-Regular',
    marginBottom: 8, textAlign: 'center',
  },
  taglineEn: {
    fontSize: 13, color: 'rgba(255,255,255,0.6)',
    fontFamily: 'Poppins-Regular', letterSpacing: 1,
    marginBottom: 40,
  },
  dotsContainer: {
    flexDirection: 'row', marginBottom: 32,
  },
  progressContainer: {
    width: width * 0.7, height: 3,
    backgroundColor: 'rgba(255,255,255,0.2)',
    borderRadius: 2, overflow: 'hidden', marginBottom: 16,
  },
  progressBar: {
    height: 3, backgroundColor: '#F4A261', borderRadius: 2,
  },
  loadingText: {
    fontSize: 12, color: 'rgba(255,255,255,0.5)',
    fontFamily: 'Poppins-Regular', letterSpacing: 0.5,
  },
});

── SIDEBAR — IMPLEMENT FULLY ─────────────────────────────────────────────

File: /src/components/Sidebar.tsx

Requirements:
  - Slide in from left (translateX: -280 → 0, 300ms ease-out)
  - Semi-transparent overlay closes drawer on tap
  - User profile section at top (avatar, name, phone)
  - Navigation items with icons:
      🏠 Home
      📋 My Bookings
      🤖 Agent Logs
      ⚙️  Settings
      🌙 Dark Mode toggle (inline switch)
      📞 Support
      ℹ️  About KHIDMAT AI
  - KHIDMAT AI branding at bottom
  - Active item highlighted in primary color
  - Each item has entrance animation (stagger 50ms per item)
  - Smooth exit animation when closing

Implementation using @react-navigation/drawer:

File: /src/navigation/DrawerNavigator.tsx

import { createDrawerNavigator } from '@react-navigation/drawer';
import { useTheme } from '../theme/ThemeContext';
import { Sidebar } from '../components/Sidebar';

const Drawer = createDrawerNavigator();

export const DrawerNavigator = () => {
  const { colors, isDark } = useTheme();
  
  return (
    <Drawer.Navigator
      drawerContent={(props) => <Sidebar {...props} />}
      screenOptions={{
        headerShown: false,
        drawerStyle: {
          backgroundColor: colors.surface,
          width: 280,
        },
        overlayColor: 'rgba(0,0,0,0.5)',
        drawerType: 'slide',
        swipeEdgeWidth: 50,
      }}
    >
      <Drawer.Screen name="Home" component={HomeScreen} />
      <Drawer.Screen name="Bookings" component={BookingHistoryScreen} />
      <Drawer.Screen name="AgentLogs" component={AgentLogsScreen} />
      <Drawer.Screen name="Settings" component={SettingsScreen} />
      <Drawer.Screen name="Profile" component={ProfileScreen} />
    </Drawer.Navigator>
  );
};

── SCREEN TRANSITIONS — ADD TO ALL SCREENS ───────────────────────────────

File: /src/navigation/AppNavigator.tsx

Use these transitions for Stack navigator:

import { TransitionPresets } from '@react-navigation/stack';

screenOptions for different transitions:
  Home → Processing:
    ...TransitionPresets.ModalPresentationIOS
    (slides up from bottom — feels like AI activating)
  
  Processing → Results:
    Custom: fade + scale
    cardStyleInterpolator: ({ current }) => ({
      cardStyle: {
        opacity: current.progress,
        transform: [{
          scale: current.progress.interpolate({
            inputRange: [0, 1],
            outputRange: [0.95, 1],
          })
        }]
      }
    })
  
  Results → Booking Confirm:
    ...TransitionPresets.SlideFromRightIOS
  
  Any → Dispute:
    ...TransitionPresets.ModalSlideFromBottomIOS

── SCREEN BY SCREEN — FIX AND ADVANCE ALL ────────────────────────────────

SCREEN 1 — SPLASH SCREEN:
  - Show LoadingScreen component for 3.5 seconds
  - Auto-navigate to Onboarding (first launch) or Home (returning)
  - Check AsyncStorage for 'khidmat_onboarded' key
  - Ensure no flicker between splash and first screen
  
  Fix: Use SplashScreen.preventAutoHideAsync() from expo-splash-screen
  Only hide native splash when fonts are loaded AND 3.5s timer done

SCREEN 2 — HOME SCREEN:
  Advanced elements to add/fix:
  
  AnimatedHeader with:
    - Greeting based on time: صبح بخیر / دوپہر بخیر / شام بخیر
    - User name from auth
    - Notification bell icon (badge if pending)
    - Hamburger menu (opens sidebar)
  
  Voice Input Button:
    - Circular mic button, 72px, accent color
    - Tap: starts recording, button pulses red with waveform animation
    - Release: sends audio to STT, fills text input
    - Long press hint: "تھامے رکھیں بولنے کے لیے"
    - Must use expo-av for recording
    - Send to Google Speech-to-Text or Whisper API
  
  Text Input:
    - Multiline, auto-expand
    - Placeholder rotates every 3s between examples:
        "AC technician chahiye..."
        "پلمبر کی ضرورت ہے..."  
        "Need an electrician urgently..."
    - Character count shown after 50 chars
    - Clear button (X) appears when text entered
    - Language auto-detect badge updates as user types
      (debounced 500ms, calls lightweight lang-detect function)
  
  Quick Service Chips (horizontal scroll):
    🌡️ AC Repair  🔧 Plumber  ⚡ Electrician
    📚 Tutor  💅 Beautician  🔨 Carpenter
    🚗 Driver  🧹 Cleaning
    Each chip: tap fills input + shows relevant icon
  
  Recent Bookings (last 3):
    Card with provider name, service, date, status badge
    Tap → navigate to TrackingScreen
  
  Submit Button:
    Full width, gradient background, 56px height
    Text: "خدمت تلاش کریں" / "Find Service"
    Loading state: spinner + "AI Agents Working..."
    Disabled state when input is empty

SCREEN 3 — PROCESSING SCREEN (MOST IMPORTANT):
  This screen must be visually stunning for the demo.
  
  Background: Animated gradient mesh
  
  Center top: "AI Agents Working..." heading with shimmer effect
  
  Agent Progress List (vertical):
  Each agent card shows:
    [Status Icon] [Agent Name] [Status Text]    [Time taken]
    
    Pending  →  ⬜ grey circle + "Waiting..."
    Active   →  🔵 spinning + agent name in primary color + "Processing..."
    Done     →  ✅ green + agent name + "Complete" + "0.8s"
    Failed   →  ❌ red + "Failed - Retrying..."
  
  Agent sequence with timing:
    INTENT_AGENT       → "Understanding your request..."
    DISCOVERY_AGENT    → "Finding providers near G-13..."
    MATCHING_AGENT     → "Ranking by 8 factors..."
    PRICING_AGENT      → "Calculating best price..."
    BOOKING_AGENT      → "Securing your slot..."
  
  Below agent list:
    Live reasoning text (streams from API):
    "Provider A ranked higher due to 94% reliability score..."
    Text typewriter effect, 30ms per character
  
  Network visualization (optional but impressive):
    Animated lines connecting agent nodes
    Nodes light up as each agent activates
    Like a neural network diagram
  
  Cancel button (bottom): "Cancel Request"

SCREEN 4 — RESULTS SCREEN:
  Fix and advance:
  
  Header: "Best Matches Found" with provider count badge
  
  Top Provider (featured card - larger):
    Provider avatar (colored circle with initials)
    Name, rating stars, review count
    Distance badge + ETA
    "⭐ Best Match" crown badge
    Specialization tags (chips)
    Key reason text: "Chosen for 96% reliability"
    Price range: "PKR 2,200 - 2,600"
    [Book Now] button - primary, full width
    [View Details] - secondary
  
  Other Options (2 cards below, smaller):
    Same info, more compact
    [Select] button
  
  Expandable "Why this provider?" panel:
    Shows scoring breakdown as horizontal bars:
      Distance      ████████░░ 16/20
      Availability  ██████████ 20/20
      Rating        ████████░░ 13/15
      Reliability   ██████████ 15/15
      Specialization ████████░░ 8/10
      Price Match   ████████░░ 8/10
      Cancellation  █████████░ 5/5
      Preference    ████░░░░░░ 2/5
      TOTAL SCORE   91/100
    
    Reasoning text in plain language
  
  Price Breakdown Accordion (expandable):
    Base fee:          PKR 1,500
    Distance (2.3km):  PKR  345
    Urgency (medium):  PKR  225
    ─────────────────────────
    Estimated Total:   PKR 2,070
    Range:       2,070 - 2,300
    
    "Budget Alternative Available" banner if applicable

SCREEN 5 — BOOKING CONFIRMATION SCREEN:
  Fix and advance:
  
  Animated success state:
    Checkmark draws itself (SVG stroke animation)
    Confetti burst (use react-native-confetti-cannon)
    "Booking Confirmed!" fades in
  
  Booking Receipt Card (the most important element):
    ┌─────────────────────────────────┐
    │ 🎫 KHIDMAT AI BOOKING RECEIPT  │
    │ ─────────────────────────────── │
    │ Booking ID:  BK-281947         │
    │ Service:     AC Repair         │
    │ Provider:    Ali AC Services   │
    │ Provider ☎:  +92-XXX-XXXXXXX   │
    │ Date:        Jan 16, 2025      │
    │ Time:        10:00 AM          │
    │ Location:    G-13, Islamabad   │
    │ ─────────────────────────────── │
    │ Estimated:   PKR 2,070 - 2,300 │
    │ Status:      ✅ CONFIRMED       │
    └─────────────────────────────────┘
    
    [Share Receipt] button → Share sheet
    [Add to Calendar] → expo-calendar
    [Download PDF] → generates and saves PDF
  
  Simulated WhatsApp Notification Preview:
    WhatsApp-styled bubble:
    "✅ Your AC Repair is confirmed for Jan 16 at 10:00 AM.
     Provider: Ali AC Services. 
     We'll remind you 1 hour before. - KHIDMAT AI"
    Send time stamp, green ticks
  
  Booking Timeline:
    ✅ Confirmed (now)
    ⏰ Reminder (9:00 AM tomorrow)
    🔧 In Progress (10:00 AM)
    ✅ Completed (~12:00 PM)
  
  "What happens next" section with steps

SCREEN 6 — TRACKING SCREEN:
  Fix and advance:
  
  Verify this screen receives booking_id and loads from Firestore
  Add real-time listener: onSnapshot for booking document
  Status updates in real-time without refresh
  
  Map view (if provider has lat/lng):
    Google Maps embedded showing:
    - User location (blue dot)
    - Provider location (teal marker)
    - Route between them
  
  Status Timeline (animated):
    Each step slides in when status updates:
    ● Booking Confirmed
    ● Reminder Sent
    ● Provider En Route → "Ali is 2.1km away"
    ● Service In Progress
    ● Service Completed
  
  Contact Provider (simulated):
    [📞 Call Provider] → Linking.openURL('tel:...')
    [💬 WhatsApp] → Linking.openURL('whatsapp://...')
  
  Feedback Form (appears when status = "completed"):
    Star rating (1-5) with tap animation
    Text input: "How was the service?"
    [Submit Feedback] → calls onFeedbackSubmit function
    Success toast: "Thank you! Provider rating updated."
  
  [Report Issue] button → Dispute Screen

SCREEN 7 — AGENT LOGS SCREEN (FOR JUDGES):
  Fix and advance:
  
  Header: "Antigravity Agent Trace"
  Filter chips: ALL | INTENT | DISCOVERY | MATCHING | PRICING | BOOKING
  
  Each log entry card:
    Agent name badge (colored by agent)
    Timestamp
    Duration (e.g., "0.8s")
    Input summary (truncated, expandable)
    Output summary (truncated, expandable)
    Tool calls used (chips: Claude API, Maps API, Firestore)
    Reasoning trace (collapsible JSON view)
  
  [Export Logs] button → share JSON file
  [Clear Logs] button → confirmation dialog
  
  Empty state: "No agent sessions yet. Make a service request first."

SCREEN 8 — SETTINGS SCREEN:
  Sections:
  
  Appearance:
    Dark/Light Mode toggle (with live preview)
    Language preference: English / اردو / Roman Urdu
    Font size: Normal / Large
  
  Account:
    Profile name, phone
    Default location
    Notification preferences
  
  About:
    App version
    Powered by Google Antigravity + Claude AI + Gemini
    GitHub repository link
    Hackathon submission info
  
  Developer (for judges):
    View API endpoints
    Test individual agents
    Reset mock data
    View Firestore collections

── BOOKING RECEIPT & TRACKING — VERIFY WORKING ──────────────────────────

CRITICAL CHECK — Receipt must:
  1. Display after successful booking (not before confirmation)
  2. All fields populated from actual API response (not hardcoded)
  3. booking_id format: BK-XXXXXX (6 random alphanumeric)
  4. Price shows range (quote * 0.9 to quote * 1.1)
  5. Share button works (React Native Share API)
  6. PDF generation works (expo-print or react-native-html-to-pdf)

CRITICAL CHECK — Tracking must:
  1. Load booking from Firestore by booking_id (not from nav params only)
  2. Real-time updates via onSnapshot listener
  3. Unsubscribe from listener when screen unmounts (prevent memory leak)
  4. Show error state if booking not found
  5. Status timeline updates within 2 seconds of Firestore change

── NOTIFICATIONS — VERIFY WORKING ───────────────────────────────────────

  Install: npx expo install expo-notifications
  
  Request permissions on first booking:
    const { status } = await Notifications.requestPermissionsAsync();
  
  Schedule local notification for reminder:
    await Notifications.scheduleNotificationAsync({
      content: {
        title: "⏰ KHIDMAT AI Reminder",
        body: "Your AC technician arrives in 1 hour! (10:00 AM)",
        data: { bookingId: 'BK-XXXXX' },
      },
      trigger: {
        date: reminderTime, // 60 min before appointment
      },
    });
  
  Verify: Notification appears in device notification tray

── ERROR BOUNDARY — IMPLEMENT EVERYWHERE ────────────────────────────────

File: /src/components/ErrorBoundary.tsx

class ErrorBoundary extends React.Component {
  state = { hasError: false, error: null };
  
  static getDerivedStateFromError(error) {
    return { hasError: true, error };
  }
  
  componentDidCatch(error, errorInfo) {
    console.error('ErrorBoundary caught:', error, errorInfo);
    // Log to Firebase Crashlytics in production
  }
  
  render() {
    if (this.state.hasError) {
      return (
        <View style={styles.errorContainer}>
          <Text style={styles.errorEmoji}>⚠️</Text>
          <Text style={styles.errorTitle}>Something went wrong</Text>
          <Text style={styles.errorMessage}>{this.state.error?.message}</Text>
          <TouchableOpacity onPress={() => this.setState({ hasError: false })}>
            <Text style={styles.retryButton}>Try Again</Text>
          </TouchableOpacity>
        </View>
      );
    }
    return this.props.children;
  }
}

Wrap EVERY screen with <ErrorBoundary> in AppNavigator

==========================================================================
PHASE 3 — UI/UX SYSTEM POLISH
==========================================================================

── ANIMATIONS CHECKLIST ─────────────────────────────────────────────────

Implement ALL of these — check each off:

  □ Splash screen: logo scale + fade entrance
  □ Splash screen: dots bounce animation  
  □ Splash screen: progress bar fills
  □ Home: header slides down on mount
  □ Home: quick chips slide in staggered (50ms each)
  □ Home: voice button pulses while recording
  □ Processing: agents activate one by one
  □ Processing: reasoning text types out character by character
  □ Results: cards slide up staggered from bottom
  □ Results: score bars animate width from 0 to value
  □ Results: provider card scales up on press (0.97 scale)
  □ Booking confirm: checkmark draws itself
  □ Booking confirm: confetti burst fires once
  □ Booking confirm: receipt card slides up
  □ Tracking: timeline items fade in as status updates
  □ Sidebar: slides from left with overlay fade
  □ Sidebar: items stagger entrance (50ms each)
  □ All buttons: press animation (scale 0.95)
  □ All cards: subtle shadow lift on press
  □ Theme toggle: smooth color transition (300ms)
  □ Tab/screen transitions: as defined in navigator

── TYPOGRAPHY SYSTEM ────────────────────────────────────────────────────

File: /src/theme/typography.ts

export const typography = {
  h1: { fontFamily: 'Poppins-Bold', fontSize: 28, lineHeight: 36 },
  h2: { fontFamily: 'Poppins-Bold', fontSize: 22, lineHeight: 30 },
  h3: { fontFamily: 'Poppins-SemiBold', fontSize: 18, lineHeight: 26 },
  h4: { fontFamily: 'Poppins-SemiBold', fontSize: 16, lineHeight: 24 },
  body: { fontFamily: 'Poppins-Regular', fontSize: 14, lineHeight: 22 },
  bodySmall: { fontFamily: 'Poppins-Regular', fontSize: 12, lineHeight: 18 },
  caption: { fontFamily: 'Poppins-Regular', fontSize: 11, lineHeight: 16 },
  urduLarge: { fontFamily: 'NotoNastaliqUrdu-Regular', fontSize: 20 },
  urduBody: { fontFamily: 'NotoNastaliqUrdu-Regular', fontSize: 16 },
  mono: { fontFamily: 'Courier', fontSize: 12 },
};

── SKELETON LOADERS ─────────────────────────────────────────────────────

File: /src/components/SkeletonLoader.tsx

Use animated shimmer effect (NOT spinner) for:
  - Provider cards loading
  - Booking history loading
  - Agent logs loading
  
  Shimmer: background gradient slides from left to right
  Colors: 
    Light: #E2E8F0 → #F7FAFC → #E2E8F0
    Dark:  #21262D → #30363D → #21262D

── TOAST NOTIFICATIONS ──────────────────────────────────────────────────

File: /src/components/ToastMessage.tsx

4 variants:
  success  → green, checkmark icon
  error    → red, X icon  
  warning  → amber, warning icon
  info     → blue, info icon

Behavior:
  Slides down from top of screen
  Auto-dismisses after 3 seconds
  Can be dismissed by swipe up
  Queue: shows one at a time

Usage throughout app:
  showToast('success', 'Booking confirmed!', 'BK-281947 created')
  showToast('error', 'No providers found', 'Try different time')
  showToast('info', 'Low confidence', 'Please clarify your request')

==========================================================================
PHASE 4 — APK BUILD & DELIVERY
==========================================================================

── PRE-BUILD CHECKLIST ──────────────────────────────────────────────────

Before building APK, verify ALL of these:

  □ All API keys in .env file and loaded correctly
  □ google-services.json in /android/app/
  □ app.json has correct package name: com.khidmatai.app
  □ app.json has correct version: "1.0.0"
  □ app.json has correct versionCode: 1
  □ All screens render without errors on emulator
  □ All 6 stress-test scenarios pass on emulator
  □ No console.error in logs during full pipeline run
  □ Firebase Auth working (anonymous auth for demo)
  □ Firestore reads/writes working
  □ All agents return correct responses
  □ Offline error handling shows friendly message

── BUILD COMMANDS ───────────────────────────────────────────────────────

For Expo managed workflow:

  OPTION A — EAS Build (Recommended for hackathon):
    Install EAS CLI: npm install -g eas-cli
    Login: eas login
    Configure: eas build:configure
    Build APK: eas build --platform android --profile preview
    
    eas.json configuration:
    {
      "build": {
        "preview": {
          "android": {
            "buildType": "apk",
            "env": {
              "EXPO_PUBLIC_API_URL": "[your orchestrator URL]",
              "EXPO_PUBLIC_MAPS_API_KEY": "[your maps key]"
            }
          }
        },
        "production": {
          "android": {
            "buildType": "app-bundle"
          }
        }
      }
    }
    
    After build: EAS provides direct APK download URL
    Download APK to local machine

  OPTION B — Local build (if EAS unavailable):
    npx expo run:android --variant release
    APK location: android/app/build/outputs/apk/release/app-release.apk
    
    If build fails: cd android && ./gradlew assembleRelease --stacktrace
    Fix any ProGuard or dependency issues shown in stacktrace

── APK TESTING BEFORE UPLOAD ────────────────────────────────────────────

  Install APK on physical Android device:
    adb install app-release.apk
  
  Test EVERY screen on device:
    □ Loading screen animates correctly
    □ Home screen voice input works
    □ Request submitted and processed
    □ Agent trace screen shows all 5 agents
    □ Results show ranked providers with scores
    □ Booking confirmed and receipt shows
    □ Tracking screen loads booking from Firestore
    □ Sidebar opens and closes smoothly
    □ Dark/Light mode toggle works and persists
    □ All fonts render correctly
    □ Arabic/Urdu text renders correctly (RTL)
    □ No ANR (App Not Responding) dialogs
    □ No crashes on any user flow
    □ Back button behavior correct on all screens
  
  Test on MINIMUM Android API 28 (Android 9)
  Test on MINIMUM screen size: 5.0 inch / 360dp width

── GITHUB UPLOAD ────────────────────────────────────────────────────────

  Commands to run in project root:

  git init (if not already)
  git remote add origin https://github.com/[your-username]/khidmat-ai-hackathon.git
  
  Create .gitignore with:
    node_modules/
    .expo/
    .env
    *.keystore
    android/.gradle/
    android/build/
    google-services.json
    
  NOTE: Never commit .env or google-services.json to public repo
  Use GitHub Secrets or mention in README to add own keys
  
  git add .
  git commit -m "feat: complete KHIDMAT AI production build v1.0.0
  
  - Full multi-agent pipeline (7 agents)
  - Flutter/React Native mobile app with dark/light theme
  - Sidebar navigation with smooth transitions
  - Loading screen with animations
  - Booking receipt and tracking
  - Dispute handling workflow
  - All stress-test scenarios passing
  - Signed APK in /releases/"
  
  git push origin main
  
  Create GitHub Release:
    Tag: v1.0.0
    Title: "KHIDMAT AI v1.0.0 - Hackathon Submission"
    Attach: app-release.apk
    Description: paste from README

── GOOGLE DRIVE UPLOAD ──────────────────────────────────────────────────

  Create folder: "KHIDMAT AI - Hackathon Submission"
  
  Upload these files:
    📦 KhidmatAI-v1.0.0.apk         ← the APK
    📹 demo-video.mp4                ← 4 min demo
    📄 README.pdf                    ← documentation
    📊 agent-trace-logs.json         ← Antigravity logs export
    🏗️  architecture-diagram.png     ← system diagram
  
  Set sharing: "Anyone with link can view"
  Copy shareable link for submission form
  
  Organize APK folder as:
    /KHIDMAT AI Submission
      /APK
        KhidmatAI-v1.0.0.apk
        install-instructions.txt
      /Source Code
        [link to GitHub repo]
      /Demo
        demo-video.mp4
        screenshots/
      /Documentation
        README.pdf
        agent-logs.json
        architecture.png

==========================================================================
PHASE 5 — FINAL VERIFICATION CHECKLIST
==========================================================================

Run through EVERY item. Fix any that fail.

── APP FUNCTIONALITY ─────────────────────────────────────────────────────
  □ App launches without crash
  □ Loading screen shows and transitions correctly
  □ Onboarding shows on first launch only
  □ Voice input captures and transcribes speech
  □ Text input accepts Urdu, Roman Urdu, English
  □ Language detection badge updates correctly
  □ Quick chips fill input and submit correctly
  □ Processing screen shows all 5 agents activating
  □ Agent reasoning text streams in
  □ Results show 3 ranked providers
  □ Provider scores and reasoning visible
  □ Price breakdown shown and correct
  □ Book Now button works
  □ Booking confirmation shows with receipt
  □ Booking saved to Firestore
  □ Tracking screen shows booking status
  □ Status updates in real-time via Firestore listener
  □ Feedback form submits and updates provider rating
  □ Dispute screen creates dispute record
  □ Sidebar opens/closes with animation
  □ All sidebar navigation items work
  □ Dark mode applies to ALL screens
  □ Light mode applies to ALL screens
  □ Theme persists after app restart
  □ Agent logs screen shows trace data
  □ Logs filterable by agent name
  □ Settings screen saves preferences

── PIPELINE ─────────────────────────────────────────────────────────────
  □ INTENT_AGENT parses Roman Urdu correctly
  □ INTENT_AGENT returns confidence score
  □ INTENT_AGENT asks clarification when needed
  □ PROVIDER_DISCOVERY_AGENT returns >= 5 providers
  □ MATCHING_AGENT closest != always #1
  □ MATCHING_AGENT shows 8-factor breakdown
  □ PRICING_AGENT returns full breakdown
  □ PRICING_AGENT handles emergency urgency correctly
  □ BOOKING_AGENT prevents double booking
  □ BOOKING_AGENT writes to Firestore
  □ FOLLOWUP_AGENT schedules reminder
  □ DISPUTE_AGENT handles no-show
  □ DISPUTE_AGENT handles price dispute
  □ All agent calls logged to agent_logs collection
  □ Full pipeline < 15 seconds end-to-end

── STRESS TEST SCENARIOS ────────────────────────────────────────────────
  □ Scenario A: Happy path — works end-to-end
  □ Scenario B: No providers — graceful error + waitlist
  □ Scenario C: Ambiguous input — clarification asked
  □ Scenario D: Provider cancels — auto-reschedule
  □ Scenario E: Price dispute — refund simulated
  □ Scenario F: Double booking — conflict prevented

── APK ──────────────────────────────────────────────────────────────────
  □ APK installs on Android 9+ without error
  □ APK runs all functionality offline-gracefully
  □ APK file size < 100MB
  □ APK signed (not debug)
  □ APK uploaded to Google Drive
  □ APK attached to GitHub release
  □ Google Drive link accessible (anyone with link)

==========================================================================
FINAL INSTRUCTION TO ALL AGENTS AND SYSTEMS
==========================================================================

You are building KHIDMAT AI for a 2.5 million rupee hackathon prize.
Every line of code matters. Every bug costs points.

RULES:
  1. Never leave a TODO or placeholder in production code
  2. Every API call has error handling
  3. Every screen has loading state AND error state AND empty state
  4. Every agent step is logged in Firestore
  5. The APK must work on a real Android phone
  6. The demo video must show all 6 scenarios
  7. The judges will open the APK on their own phone — it must work
  8. The sidebar, loading screen, dark mode, and transitions must be smooth
  9. The receipt must look like a real receipt
 10. The agent trace screen must clearly show AI reasoning

When in doubt: build it, test it, fix it, test again.
Ship when all 50 checklist items pass.

GO WIN THIS.
==========================================================================